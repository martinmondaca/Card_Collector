<div class="container" id="currentSetTable">
    <table class="highlight">
        <thead>
            <tr>
                <th>Card Number</th>
                <th>Name</th>
                <th>Set Name</th>
                <th>Set Year</th>
                <th>Own?</th>
            </tr>
        </thead>

        <tbody>
            {{#each cards}}
            <tr>
                <td>{{this.cardnumber}}</td>
                <td>{{this.name}}</td>
                <td>{{this.setname}}</td>
                <td>{{this.cardyear}}</td>
                <td>
                    <form>
                        {{#if checked}}
                        <label for={{this.id}}>
                            <input checked="checked" class="checkbox" type="checkbox" class="filled-in" id={{this.id}} />
                            <span></span>
                        </label>
                        {{else}}
                        <label for={{this.id}}>
                            <input class = "checkbox" type="checkbox" class="filled-in" id={{this.id}} />
                            <span></span>
                        </label>
                        {{/if}}
                    </form>
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>


</div>

<br>
<div id="makePDF" class="container center-align">
    <a id="downloadPDF" class="waves-effect waves-orange btn blue center-align"><i
            class="material-icons right">file_download</i>Download
        PDF</a>
</div>